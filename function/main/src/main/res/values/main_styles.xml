<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/main_shape_splash_bg</item>
    </style>

    <style name="SplashAdDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/main_dialog_enter_splash_ad</item>
        <item name="android:windowExitAnimation">@anim/main_dialog_exit_splash_ad</item>
    </style>
</resources>